export default function StaffDashboard() {
    return (
        <div className="dashboard-container">
            <h2 className="dashboard-title">Staff Dashboard</h2>
            <p className="dashboard-text">View products & update stock quantity only.</p>
        </div>
    );
}